# changelog-py
Generate changelog from conventional commits.
```bash
python changelog.py --version v1.2.0
python changelog.py --since v1.1.0 -o CHANGELOG.md
python changelog.py --all
```
## Zero dependencies. Python 3.6+.
